// TOMBOL FOOTER ON HOVER
$('.insta.btn-app').mousemove(() => {
    $('.insta.btn-app img').css('filter', 'invert(1)');
})

$('.insta.btn-app').mouseleave(() => {
    $('.insta.btn-app img').css('filter', 'unset');
})

$('.youtube.btn-app').mousemove(() => {
    $('.youtube.btn-app img').css('filter', 'invert(1)');
})

$('.youtube.btn-app').mouseleave(() => {
    $('.youtube.btn-app img').css('filter', 'unset');
})

$('.linkedin.btn-app').mousemove(() => {
    $('.linkedin.btn-app img').css('filter', 'invert(1)');
})

$('.linkedin.btn-app').mouseleave(() => {
    $('.linkedin.btn-app img').css('filter', 'unset');
})

$('.line.btn-app').mousemove(() => {
    $('.line.btn-app img').css('filter', 'invert(1)');
})

$('.line.btn-app').mouseleave(() => {
    $('.line.btn-app img').css('filter', 'unset');
})

$('.tiktok.btn-app').mousemove(() => {
    $('.tiktok.btn-app img').css('filter', 'invert(1)');
})

$('.tiktok.btn-app').mouseleave(() => {
    $('.tiktok.btn-app img').css('filter', 'unset');
})
